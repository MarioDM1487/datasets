{
  "name": "tepore-app",
  "private": true,
  "version": "0.1.0",
  "description": "Tepore — app nativa (Capacitor) per il monitoraggio comfort cavallo",
  "scripts": {
    "build:ble": "esbuild src/native-ble.js --bundle --format=iife --outfile=www/native-ble.js",
    "build:web": "node build-web.js",
    "build": "npm run build:ble && npm run build:web",
    "sync": "npm run build && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor-community/bluetooth-le": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "esbuild": "^0.24.0"
  }
}
